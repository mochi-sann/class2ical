import { Button } from "@chakra-ui/react";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <div>
      <Button>This is Button</Button>
    </div>
  );
};

export default Home;
